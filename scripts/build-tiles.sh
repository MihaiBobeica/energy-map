#!/usr/bin/env bash
# Placeholder: PMTiles generation lands in Phase 4 (plants) and Phase 5
# (admin-1). This script will wrap tippecanoe + pmtiles conversion for
# countries, admin1, urban-centres and plants layers, writing into
# public/data/tiles/. It intentionally fails until then so nothing can
# depend on unbuilt tiles by accident.
set -euo pipefail

echo "build-tiles: not implemented yet — scheduled for Phase 4 (see docs/requirements.md §6)." >&2
exit 2
